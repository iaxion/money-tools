# 自動運用ガイド（夜間・無人でツールを増やすための手順書）

このファイルは「スケジュール実行のエージェント」や次回以降の作業セッションが、
**毎回ここを読んで1つずつツールを追加・改善していく**ための作業指示書です。
人間の確認なしで安全に回せるよう、作業手順とガードレールを定義します。

---

## 🤖 1回の自動実行でやること（ランブック）

1. `git pull`（あれば）で最新化する。
2. 下の **バックログ** から、未完(`[ ]`)の最優先項目を **1つ** 選ぶ。
3. 計算ロジックは `src/lib/` に**純粋関数**で実装する。
4. 実装直後に簡易チェックを走らせ、既知の概算値と大きくズレないか確認する：
   ```sh
   node --experimental-strip-types <一時テスト.mjs>
   ```
5. ページを `src/pages/tools/<slug>.astro` に作成する。既存の `tools/tedori.astro` を雛形にし、
   **必ず**以下を踏襲：`Base` レイアウト / `path` / `description` / JSON-LD(BreadcrumbList + FAQPage) /
   「概算についての注意」/ 末尾に内部リンク。
6. `src/pages/index.astro` のツール一覧で該当項目を `ready: true` にする。
7. `npm run build` が**通ること**を確認する（通らなければ直してから次へ）。
8. このファイルのバックログにチェック `[x]` を入れ、コミットする：
   ```sh
   # コミット時の識別子はリポジトリの git config を使う（個人メールを直書きしない）
   git add -A && git commit -m "ツール追加: <名称>"
   ```
   （GitHub 連携済みなら自動デプロイで公開される）

---

## 🛡 ガードレール（絶対に守る）

- **お金をマイナスにしない**：有料サービスを勝手に契約しない。インフラは無料枠
  （Cloudflare Pages 無料・取得済みドメイン）のみ。課金が必要な施策は実行せず README に提案として書くだけ。
- **日本の法律・規約を順守**：誇大表現・無断転載・スパム的大量投稿・自動アカウント作成はしない。
- **正確性が最優先（YMYL）**：税率・保険料率は推測で書かない。国税庁・協会けんぽ等の**公式値を確認**してから反映。
  不確かな場合は「概算」と明記し、確定情報が取れるまで保守的にする。
- **1実行=1ツール程度**に留め、必ずビルドが通る状態でコミットする（壊れた状態を残さない）。
- 既存ツールの計算結果を変える変更をしたら、簡易チェックで妥当性を再確認する。

---

## 📋 バックログ（上から優先）

- [x] 給与の手取り計算（`tools/tedori`）
- [x] 積立NISA・投資シミュレーター（`tools/nisa`）— 毎月積立額×利回り×年数の複利計算。証券口座アフィリと好相性。
- [ ] ボーナス（賞与）の手取り計算（`tools/bonus`）— 賞与の社会保険料＋源泉徴収税額表ベース。
- [x] ふるさと納税 限度額シミュレーター（`tools/furusato`）— 年収・家族構成から控除上限の目安。
- [x] 住宅ローン返済シミュレーター（`tools/loan`）— 元利均等返済の月返済額・総返済額・利息。
- [x] 年収・月収・時給 換算ツール（`tools/kanzan`）。
- [x] 退職金の手取り計算（`tools/taishoku`）— 退職所得控除・1/2課税。
- [ ] 副業の確定申告 要否・税額シミュレーター（`tools/fukugyo`）— 会計ソフトアフィリと好相性。
- [ ] 各ツールに「年収別 早見表」等のSEO補強ページを追加。
- [ ] 料率の年度更新チェック（毎年3月: 協会けんぽ、毎年: 税制改正）。

---

## 🩺 定期レビュー：料率・税制の鮮度チェック（最重要・自動化対象）

税率・社会保険料率は毎年改定される（**社会保険＝毎年4月**、**税制改正＝年末大綱→翌年施行**）。
古い値のまま気づかないと金融ツールとして致命的。これを**スケジュール実行で定期的に自動チェック**する。

**いつ:** 毎月1回（特に **3〜4月**＝社会保険改定期、**12〜1月**＝税制改正期は要注意）。
鮮度メタは `src/lib/freshness.ts` の `TAX_DATA_META`（`fiscalYear` / `lastReviewed` / `reviewDue`）。

**1回のレビュー手順:**
1. 現在日（システムの現在日付）を確認する。`reviewDue` を過ぎている／改定期なら重点的に。
2. 公式値をWeb検索で取得して `src/lib/tax.ts` の `RATES`・控除関数と突き合わせる：
   - 協会けんぽ 東京：当年度の健康保険料率・介護保険料率・**子ども子育て支援金**率（`https://www.kyoukaikenpo.or.jp/`）
   - 雇用保険料率（当年度・一般の事業の労働者負担）
   - 国税庁：当年分の基礎控除（合計所得別）・給与所得控除の最低保障（`https://www.nta.go.jp/`）
3. **差異があれば**：`tax.ts` の該当値・控除関数を更新 → 各ページの「令和◯年／◯年」表記も更新 →
   `freshness.ts` の `fiscalYear`・`lastReviewed`（今日）・`reviewDue`（翌年4/1）を更新。
4. 簡易テストで手取りが妥当か確認（既知の公表値と照合）→ `npm run build` 通過を確認。
5. コミット＆push（auto-deploy）。**差異が無ければ** `lastReviewed` だけ今日に更新してコミット（「確認済み」の記録）。
6. **不確かな場合は変更しない**（保守的に）。公式の確定値が取れるまで待ち、必要なら課題として残す。

**ガードレール:** 公式情報源のみ。推測で料率を書かない。ビルドが通る状態でのみコミット。

## 🔁 起動方法（2通り）

### A. クラウドのスケジュール実行（Mac の電源と無関係・本命）
GitHub 連携後に、Claude Code のスケジュール機能（routine / cron）で
「このリポジトリで AUTONOMY.md のランブックを実行」を毎晩走らせる。
1回の実行が利用上限に達しても、**次回のスケジュール実行で自然に続きから再開**する。

### B. ローカルのループ（Mac を起動したまま回す場合）
Mac を起こし続けてこのセッションで回す場合：
```sh
caffeinate -dimsu &     # スリープ防止（電源接続推奨）
```
その上で `/loop` 等で定期実行する。Mac を閉じると止まるため、無人運用は A を推奨。

---

## 📈 収益化の進め方（フェーズ）

- **フェーズ0（今）**：v1公開 → AdSense/アフィリ申請 → **最初の1円**。
- **フェーズ1**：ツール5〜8個＋SEO補強、SNS集客 → **月1万円**。
- **フェーズ2**：ツール15個以上＋上位表示＋高単価アフィリ最適化 → **月10万円**。

具体的な手動セットアップ手順は `README.md` の「あなたが行う手動ステップ」を参照。
